<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shcoolwork.mapper.UserMapper">

    <insert id="add">
        insert into user
        (username, password, compus, enclosure, unit, phone, image, create_time, update_time, status, del_flag, student_id) VALUES
         (#{username},#{password},#{compus},#{enclosure},#{unit},#{phone},#{image},#{createTime},#{updateTime},#{status},#{delFlag},#{studentId})
    </insert>
</mapper>
